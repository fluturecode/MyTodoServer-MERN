import React from "react";
import "./Todo.css";

function Todo() {
	return (
		<div>
			<h1>Todos</h1>
			<h2>Completed</h2>
			{/* // If complete is true, mark with check. */}
		</div>
	);
}

export default Todo;

//Endpoints
//GET / POST / DELETE
// Inputs & Outputs ie. 'request' and 'response'

// C-REATE todo (POST)
// R-EAD - GET ALL todos (GET)
// U-PDATE todo (PUT)
// D-ELETE todo (DELETE)
